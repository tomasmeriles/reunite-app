info:
  name: me
  type: http
  seq: 5

http:
  method: GET
  url: '{{baseUrl}}/auth/me'
  headers:
    - name: x-tenant-id
      value: ''
      enabled: false

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
