info:
  name: logout
  type: http
  seq: 6

http:
  method: POST
  url: '{{baseUrl}}/auth/logout'
  headers:
    - name: x-csrf-token
      value: '{{csrfToken}}'
      enabled: true

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
