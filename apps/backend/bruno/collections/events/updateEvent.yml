info:
  name: update event
  type: http
  seq: 2

http:
  method: PATCH
  url: "{{baseUrl}}/events/{{eventId}}"
  headers:
    - name: x-csrf-token
      value: "{{csrfToken}}"
  body:
    type: json
    data: |-
      {
        "title": "Test Event (edit)"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
